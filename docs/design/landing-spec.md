# Landing-page design

Reference: landing-concept.png. Continue the paper #f7f6f2 / forest #245b45 palette, thin rules, small corners and restrained typography of Pass It On. Split hero, single illustrative answer, sage generosity band, four numbered process rows, provider roles, FAQ and final CTA. Route `/` is the introduction; `/app` remains the tool.

User correction: replace Georgia in workspace source headings and quotations with system sans-serif. Apply that clearer reading typography to landing-page examples and downstream sections too. Only the brand and introductory headline retain Georgia.

Accuracy overrides for generated concept copy: Snowflake stores request metadata and approved answers, not raw transcripts. Solana records a transfer and contribution memo, not the answer itself. Human review is a demo role; the example is illustrative. Provider configuration is fetched live; a pending Snowflake connection is explicitly stated. Hero source details and FAQs expand; all app links work; the correction link must preserve existing drafts.
